{
	"tasks": [
		{
			"type": "shell",
			"label": "Build",
			"command": "make build",
			"options": {
				"cwd": "${workspaceFolder}"
			},
		},
		{
			"type": "shell",
			"label": "Cleanup",
			"command": "make clean",
			"options": {
				"cwd": "${workspaceFolder}"
			},
		}
	],
	"version": "2.0.0"
}